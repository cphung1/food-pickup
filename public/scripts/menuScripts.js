const db = require('index.js');


$( document ).ready(function() {
  console.log( "ready!" );


  console.log("before the query");


});

// $(function() {

  // db.query('SELECT * FROM items;')

  // console.log("afer the query");

    // .then(data => {

//         for (let key of data.rows) {
//           let $item = createItemElement(key);
//           console.log($item);
//           $('mid_categories').append($item);
//         }
//     })
//     .catch(err => cb(err));
// };

// const createItemElement = items => {
//   const $item = `
//   <article>
//   <button type="button" data-toggle="modal" data-target="#exampleModalCenter">
//     // <h4>${tweet.user.handle}</h4>
//     <p2>$4.75</p2>
//   </button>
//   <p class="text-left">Espresso, steamed milk & a layer of foam on top. The classic milk & espresso drink.</p>
// </article>
//  `;
//   return $($item);
// };

    // })



// });
